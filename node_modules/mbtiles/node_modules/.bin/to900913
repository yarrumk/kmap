#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sphericalmercator/bin/to900913.js" "$@"
  ret=$?
else 
  node  "$basedir/../sphericalmercator/bin/to900913.js" "$@"
  ret=$?
fi
exit $ret
